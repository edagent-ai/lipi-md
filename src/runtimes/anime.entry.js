/**
 * IIFE entry for the bundled Anime.js sandbox runtime (MIT).
 *
 * Built by `vite.runtimes.config.ts` into `public/runtimes/anime.iife.js` so a
 * sandboxed, opaque-origin iframe can load it as a plain classic script.
 */
import * as anime from 'animejs';

export default anime;
